{
  "name": "titanik-package",
  "version": "1.0.0",
  "description": "Node packages for TITANIK web template",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-build": "sass ./sass/custom.sass:./css/custom.css --style compressed",
    "sass-watch": "sass --watch ./sass/:./css/ --style compressed"
  },
  "keywords": [
    "TITANIK"
  ],
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.0.1",
    "bootstrap-icons": "^1.5.0",
    "clamp-js": "^0.7.0"
  },
  "devDependencies": {
    "sass": "^1.34.1"
  }
}
